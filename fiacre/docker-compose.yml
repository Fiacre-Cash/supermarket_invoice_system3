version: "3.8"

services:
  web:
    build: .
    ports:
      - "8080:80"
    # For local development, mount sources so changes appear immediately in the container
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
